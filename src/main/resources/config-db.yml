spring:
  datasource:
    hikari:
      minimum-idle: 1
      maximum-pool-size: 10
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: postgresql
    open-in-view: false
    show-sql: false
    hibernate:
      ddl-auto: update
